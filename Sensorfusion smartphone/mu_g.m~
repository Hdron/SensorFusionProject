function [x, P] = mu_g(x, P, yacc, Ra, g0)
   
    h = Qq(x)
    [Q0, Q1, Q2, Q3] = dQqdq(x)
    H = [Q0, Q1, Q2, Q3]
    
    S = H*P()*H' + Ra;
    K = P*H'*inv(S);
    
    x = x + K*(yacc-h)
    P = P - K*S*K'

end

